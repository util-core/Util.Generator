@* 领域层仓储接口 *@
@using Util.Generators.Helpers
@model Util.Generators.Contexts.EntityContext
@{
    var service = new GenerateService( Model );
    var repository = $"I{service.EntityName}Repository";
    var description = Model.Description;
}
using Util.Domain.Trees;
using @service.GetDomainNamespace( "Models" );

namespace @service.GetDomainNamespace( "Repositories" ) {
    /// <summary>
    /// @(description)仓储
    /// </summary>
    public interface @repository : @service.GetITreeRepository() {
    }
}