@* Ui层项目 - 自定义样式索引文件 *@
@using Util.Generators.Helpers
@model Util.Generators.Contexts.EntityContext
@{
    var service = new GenerateService( Model );
    Model.Output.FileNameNoExtension = "index";
    Model.Output.Extension = ".less";
    Model.Output.RelativeRootPath = $"src/{service.GetUiProjectName()}/ClientApp/src/styles";
}
//表格选中行背景色
.table-row-selected {
    .ant-table-cell {
        background-color: rgb(230,230,240) !important;
    }

    .ant-btn-dangerous {
        background-color: transparent;
    }
}

//查询表单卡片样式
.searchForm .ant-card-body {
    padding-bottom: 0 !important;
}

//输入框组宽度
.alain-default__content nz-input-group {
    width: 100%;
}

//表格设置按钮样式
.btn-table-settings {
    font-size: 16px;
    color: rgba(0,0,0,0.85);
}