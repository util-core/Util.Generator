@* Ui层项目 - 详情组件文件 *@
@using Util.Generators.Helpers
@model Util.Generators.Contexts.EntityContext
@{
    var service = new GenerateService( Model );
    var description = $"{Model.Description}详情页";
    Model.Output.FileNameNoExtension = service.GetClientDetailFileName();
    Model.Output.Extension = ".ts";
    Model.Output.RelativeRootPath = $"src/{service.GetClientComponentPath()}";
}
import { Component,ChangeDetectionStrategy, Injector } from '@@angular/core';
import { environment } from "@@env/environment";
import { EditComponentBase } from "util-angular";
import { @service.GetClientViewModelClassName() } from './model/@service.GetClientViewModelFileName()';

/**
* @description
*/
@@Component( {
    selector: '@service.GetClientEntityFileName()-detail',
    changeDetection: ChangeDetectionStrategy.OnPush,
    templateUrl: './html/@(service.GetClientDetailFileName()).html'
} )
export class @service.GetClientDetailClassName() extends EditComponentBase<@service.GetClientViewModelClassName()> {
    /**
     * 获取基地址
     */
    protected getBaseUrl() {
        return "@service.GetClientEntityName()";
    }
}